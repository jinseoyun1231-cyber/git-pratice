//
//  ContentView.swift
//  dlfjads1
//
//  Created by Seoyun Jin on 5/26/26.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!")
            Text("Bye, world!")
            Text("에벱베")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
